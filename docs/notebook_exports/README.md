This directory holds HTML exports of notebooks listed in `config/notebook_plan.json`, generated by `python scripts/export_html.py` (also run from `reproduce_all.py`). Filenames follow each notebook stem (for example `01_smoke_test_execute.html`).
